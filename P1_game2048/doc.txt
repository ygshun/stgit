2048游戏
1. 将之前的核心算法定义到游戏核心算法类GameCoreController
   全局变量 --> 实例变量
   函数 --> 实例方法
2. 创建生成新数字功能
    -- 随机位置(空的)
        思路：找出所有空位置,随机选择一个.
    -- 随机数字(2->90%     4-> 10%)
3. 创建判断游戏是否结束的功能
    -- 是否具有空位置(如果有 -- 不结束)
    -- 如果没有空位置，再判断横向纵向是否具有相邻相同元素
                                   (如果有 -- 不结束)
    -- 以上2个条件都不满足,游戏结束








